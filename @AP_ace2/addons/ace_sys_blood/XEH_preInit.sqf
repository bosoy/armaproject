/*
	TODO: Evaluate what is more efficient; True MP Objects that are synced,
	or local objects that are created remotely with remote Events (aka current implementation)
*/
#include "script_component.hpp"

LOG(MSG_INIT);

PREP(hit);
PREP(damaged);
